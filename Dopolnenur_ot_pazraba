-- made by dumbo3435
-- deobfuscated, fixed, & made robust by Antigravity (Google DeepMind)
-- works on both story/endless. round 1 might have some criminals passing through but should work flawlessly after that

print("Secure the Airport: Initializing UI Menu...")

local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui", 15)

-- Destroy old GUI instance if it exists to prevent overlapping UI on re-run
if CoreGui:FindFirstChild("AntigravityHub") then
    CoreGui["AntigravityHub"]:Destroy()
end
if PlayerGui:FindFirstChild("AntigravityHub") then
    PlayerGui["AntigravityHub"]:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AntigravityHub"
ScreenGui.ResetOnSpawn = false
-- Set max possible display order so it stays visible and interactive on top of the Roblox Escape menu!
ScreenGui.DisplayOrder = 2147483647 

-- Put in CoreGui to persist across deaths/resets, fallback to PlayerGui
local success, err = pcall(function()
    ScreenGui.Parent = CoreGui
end)
if not success then
    ScreenGui.Parent = PlayerGui
end

-- Simple custom dragging logic compatible with touch (mobile) and mouse (PC)
local function makeDraggable(frame, parentFrame)
    parentFrame = parentFrame or frame
    local dragging
    local dragInput
    local dragStart
    local startPos
    
    local function update(input)
        local delta = input.Position - dragStart
        parentFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
    
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = parentFrame.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    
    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    
    game:GetService("UserInputService").InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            update(input)
        end
    end)
end

-- Main UI Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 300, 0, 190)
MainFrame.Position = UDim2.new(0.5, -150, 0.35, -95)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = 2
UIStroke.Color = Color3.fromRGB(60, 60, 75)
UIStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
UIStroke.Parent = MainFrame

makeDraggable(MainFrame)

-- Title
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.new(1, 0, 0, 36)
Title.BackgroundTransparency = 1
Title.Text = "Antigravity Hub"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

local TitleLine = Instance.new("Frame")
TitleLine.Size = UDim2.new(0.9, 0, 0, 1)
TitleLine.Position = UDim2.new(0.05, 0, 0, 36)
TitleLine.BackgroundColor3 = Color3.fromRGB(50, 50, 65)
TitleLine.BorderSizePixel = 0
TitleLine.Parent = MainFrame

-- Subtitle
local Subtitle = Instance.new("TextLabel")
Subtitle.Size = UDim2.new(1, 0, 0, 20)
Subtitle.Position = UDim2.new(0, 0, 0, 42)
Subtitle.BackgroundTransparency = 1
Subtitle.Text = "Secure the Airport Autofarm"
Subtitle.TextColor3 = Color3.fromRGB(140, 140, 150)
Subtitle.TextSize = 11
Subtitle.Font = Enum.Font.Gotham
Subtitle.Parent = MainFrame

-- Toggle Button
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "ToggleBtn"
ToggleBtn.Size = UDim2.new(0.8, 0, 0, 38)
ToggleBtn.Position = UDim2.new(0.1, 0, 0.46, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(35, 140, 75) -- Default Green (Enabled)
ToggleBtn.Text = "AUTOFARM: ENABLED"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextSize = 13
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.Parent = MainFrame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 8)
BtnCorner.Parent = ToggleBtn

-- Destroy / Unload Button
local DestroyBtn = Instance.new("TextButton")
DestroyBtn.Name = "DestroyBtn"
DestroyBtn.Size = UDim2.new(0.8, 0, 0, 34)
DestroyBtn.Position = UDim2.new(0.1, 0, 0.72, 0)
DestroyBtn.BackgroundColor3 = Color3.fromRGB(150, 40, 40) -- Default Red
DestroyBtn.Text = "STOP & UNLOAD CHEAT"
DestroyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
DestroyBtn.TextSize = 12
DestroyBtn.Font = Enum.Font.GothamBold
DestroyBtn.Parent = MainFrame

local DestroyCorner = Instance.new("UICorner")
DestroyCorner.CornerRadius = UDim.new(0, 8)
DestroyCorner.Parent = DestroyBtn

-- Minimize / Floating Icon
local FloatBtn = Instance.new("TextButton")
FloatBtn.Name = "FloatBtn"
FloatBtn.Size = UDim2.new(0, 45, 0, 45)
FloatBtn.Position = UDim2.new(0.05, 0, 0.15, 0)
FloatBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
FloatBtn.Text = "AG"
FloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FloatBtn.TextSize = 14
FloatBtn.Font = Enum.Font.GothamBold
FloatBtn.Visible = false
FloatBtn.Parent = ScreenGui

local FloatCorner = Instance.new("UICorner")
FloatCorner.CornerRadius = UDim.new(0, 22.5)
FloatCorner.Parent = FloatBtn

local FloatStroke = Instance.new("UIStroke")
FloatStroke.Thickness = 2
FloatStroke.Color = Color3.fromRGB(60, 60, 75)
FloatStroke.Parent = FloatBtn

makeDraggable(FloatBtn)

-- Minimize Frame Button inside Main Menu
local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseBtn"
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(0.9, -8, 0.04, 0)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "—"
CloseBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
CloseBtn.TextSize = 14
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = MainFrame

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    FloatBtn.Visible = true
end)

FloatBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = true
    FloatBtn.Visible = false
end)

-- Script state local variables (avoids global race conditions on re-execution)
local scriptActive = true
local autofarmEnabled = true

-- Connection tracker dictionary (keys: NPC models, values: connections)
-- Prevents memory leaks by auto-garbage collecting when NPCs are destroyed
local dumbo3435_connections = {}

local function unloadCheat()
    scriptActive = false
    autofarmEnabled = false
    
    -- Disconnect all active property change connections
    for npc, conn in pairs(dumbo3435_connections) do
        if conn then
            pcall(function() conn:Disconnect() end)
        end
        -- Restore original walkspeeds to all active NPCs
        if npc and npc.Parent then
            local hum = npc:FindFirstChildOfClass("Humanoid")
            if hum then
                pcall(function() hum.WalkSpeed = 12 end)
            end
        end
    end
    table.clear(dumbo3435_connections)
    
    -- Destroy the ScreenGui completely
    if ScreenGui then
        ScreenGui:Destroy()
    end
    
    print("Secure the Airport: Cheat fully stopped and unloaded!")
end

ToggleBtn.MouseButton1Click:Connect(function()
    autofarmEnabled = not autofarmEnabled
    if autofarmEnabled then
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(35, 140, 75)
        ToggleBtn.Text = "AUTOFARM: ENABLED"
    else
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(140, 35, 35)
        ToggleBtn.Text = "AUTOFARM: DISABLED"
    end
end)

DestroyBtn.MouseButton1Click:Connect(function()
    unloadCheat()
end)

print("Secure the Airport: UI Menu initialized!");

print("Secure the Airport: Waiting for game to load...")
repeat
    task.wait();
until game:IsLoaded() 

print("Secure the Airport: Waiting for Map in workspace...")
repeat
    task.wait();
until workspace:FindFirstChild("Map") 

task.wait(1);
print("Secure the Airport: Map loaded. Initializing variables...");

local dumbo3435_13 = game:GetService("ReplicatedStorage");
local dumbo3435_14 = game:GetService("Players");
local dumbo3435_18 = dumbo3435_14["LocalPlayer"];
local dumbo3435_19 = workspace["CurrentCamera"];

local workspaceScriptable = workspace:WaitForChild("WorkspaceScriptable", 15);
if not workspaceScriptable then
    print("Secure the Airport Error: WorkspaceScriptable not found!");
    return;
end

local storage = workspaceScriptable:WaitForChild("Storage", 15);
local normalStorage = storage and storage:WaitForChild("NormalStorage", 15);

local dumbo3435_15 = normalStorage and normalStorage:WaitForChild("NPCWorkspace", 15);
local dumbo3435_16 = normalStorage and normalStorage:WaitForChild("HostileNPCWorkspace", 15);
local dumbo3435_17 = storage and storage:WaitForChild("DisasterStorage", 15);

local objects = workspaceScriptable:WaitForChild("Objects", 15);
local buttonPodiums = objects and objects:WaitForChild("ButtonPodiums", 15);

local dumbo3435_22 = buttonPodiums and buttonPodiums:WaitForChild("ButtonPodiumMD", 15);
local dumbo3435_23 = dumbo3435_22 and dumbo3435_22:WaitForChild("ButtonAccept", 15) and dumbo3435_22["ButtonAccept"]:WaitForChild("ButtonPress", 15) and dumbo3435_22["ButtonAccept"]["ButtonPress"]:WaitForChild("ProximityPrompt", 15);

local dumbo3435_24 = buttonPodiums and buttonPodiums:WaitForChild("ButtonPodiumID", 15);
local dumbo3435_25 = dumbo3435_24 and dumbo3435_24:WaitForChild("ButtonAccept", 15) and dumbo3435_24["ButtonAccept"]:WaitForChild("ButtonPress", 15) and dumbo3435_24["ButtonAccept"]["ButtonPress"]:WaitForChild("ProximityPrompt", 15);

local computers = objects and objects:WaitForChild("Computers", 15);
local dumbo3435_26 = computers and computers:WaitForChild("ComputerPower", 15);
local dumbo3435_27 = dumbo3435_26 and dumbo3435_26:WaitForChild("Highlight", 15);
local dumbo3435_28 = dumbo3435_26 and dumbo3435_26:WaitForChild("Monitor", 15) and dumbo3435_26["Monitor"]:WaitForChild("Attachment", 15);

local dumbo3435_29 = {};
local garbageBins = objects and objects:WaitForChild("GarbageBins", 15);
if garbageBins then
    table.insert(dumbo3435_29, garbageBins:WaitForChild("GarbageBin1", 15));
    table.insert(dumbo3435_29, garbageBins:WaitForChild("GarbageBin2", 15));
    table.insert(dumbo3435_29, garbageBins:WaitForChild("GarbageBin3", 15));
end

local dumbo3435_30 = objects and objects:WaitForChild("FireExtinguisherPoints", 15);
local dumbo3435_31 = dumbo3435_18:WaitForChild("PlayerGui", 15);
local dumbo3435_32 = dumbo3435_31 and dumbo3435_31:WaitForChild("EndingUI", 15);
local dumbo3435_20 = dumbo3435_13:WaitForChild("Resources", 15) and dumbo3435_13["Resources"]:WaitForChild("Events", 15) and dumbo3435_13["Resources"]["Events"]:WaitForChild("Client", 15) and dumbo3435_13["Resources"]["Events"]["Client"]:WaitForChild("BackToLobby", 15);
local dumbo3435_21 = dumbo3435_13:WaitForChild("Resources", 15) and dumbo3435_13["Resources"]:WaitForChild("Events", 15) and dumbo3435_13["Resources"]["Events"]:FindFirstChild("Client") and dumbo3435_13["Resources"]["Events"]["Client"]:WaitForChild("FireExtinguisher", 15);

-- Static CFrames obtained directly from the player's character in-game
local cframeCenter = CFrame.new(-54.345211, 3.00000024, -6.39607954, 0.60876298, 2.20848229e-08, -0.793352187, -4.42953976e-08, 1, -6.15183815e-09, 0.793352187, 3.888686e-08, 0.60876298)
local cframeMD = CFrame.new(-41.6090355, 3.00000024, -5.88942528, -0.156434447, -6.73699816e-08, -0.987688363, -5.03442728e-08, 1, -6.02360117e-08, 0.987688363, 4.03014653e-08, -0.156434447)
local cframeID = CFrame.new(-58.8363342, 3.00000024, -22.6157169, -0.0523362495, -4.32435288e-09, -0.99862951, 3.60094994e-08, 1, -6.21747587e-09, 0.99862951, -3.62855488e-08, -0.0523362495)

local dumbo3435_33 = cframeCenter.Position;

local dumbo3435_34 = {};

local pressingButtons = false;
local currentPodium = "ID"; -- Alternates between "ID" and "MD" when both have passengers
local stateStartTime = os.clock();

-- Prompt click safety cooldown trackers
local lastTriggerTimeID = 0
local lastTriggerTimeMD = 0

local function dumbo3435_35()
    for dumbo3435_36, dumbo3435_37 in dumbo3435_29 do
        if (dumbo3435_37 and dumbo3435_37:FindFirstChild("FirePart") and dumbo3435_37["FirePart"]:FindFirstChild("IsOnFire") and (dumbo3435_37["FirePart"]["IsOnFire"]["Value"] == true)) then
            return true;
        end
    end
    return false;
end

-- Bulletproof function to trigger ProximityPrompts using all available methods
local function triggerPrompt(prompt)
    if not prompt or not prompt.Enabled then return end
    
    -- Method 1: Roblox native input simulation (always works on all executors)
    task.spawn(function()
        prompt:InputHoldBegin()
        task.wait(prompt.HoldDuration + 0.02)
        prompt:InputHoldEnd()
    end)
    
    -- Method 2: Exploit-specific fireproximityprompt
    if fireproximityprompt then
        pcall(function()
            fireproximityprompt(prompt)
        end)
    end
end

local function dumbo3435_38(dumbo3435_39)
    local dumbo3435_40 = dumbo3435_18["Character"];
    if (not dumbo3435_40) then
        return nil;
    end
    local dumbo3435_41 = dumbo3435_40:FindFirstChildOfClass("Humanoid");
    if not dumbo3435_41 then
        return nil;
    end
    local dumbo3435_42 = dumbo3435_40:FindFirstChild(dumbo3435_39);
    if (dumbo3435_42) then
        return dumbo3435_42;
    end
    local dumbo3435_43 = dumbo3435_18["Backpack"]:FindFirstChild(dumbo3435_39);
    if (dumbo3435_43) then
        dumbo3435_41:EquipTool(dumbo3435_43);
        local dumbo3435_44 = 0;
        while not dumbo3435_40:FindFirstChild(dumbo3435_39) and (dumbo3435_44 < 20) do
            task.wait(0.1);
            dumbo3435_44 = dumbo3435_44 + 1;
        end
    end
    return dumbo3435_40:FindFirstChild(dumbo3435_39);
end

local function dumbo3435_45(dumbo3435_46)
    if dumbo3435_34[dumbo3435_46] then
        return;
    end
    local dumbo3435_47 = dumbo3435_46:FindFirstChild("HumanoidRootPart");
    local dumbo3435_48 = dumbo3435_46:FindFirstChildOfClass("Humanoid");
    if (not dumbo3435_47) then
        return;
    end
    dumbo3435_34[dumbo3435_46] = true;
    if (dumbo3435_48) then
        dumbo3435_48["WalkSpeed"] = 0;
        dumbo3435_48["PlatformStand"] = true;
        -- Maintain freeze on WalkSpeed change
        if not dumbo3435_46:GetAttribute("FreezeSetupDone") then
            dumbo3435_46:SetAttribute("FreezeSetupDone", true)
            local conn
            conn = dumbo3435_48:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
                if dumbo3435_48.WalkSpeed ~= 0 then
                    dumbo3435_48.WalkSpeed = 0
                end
            end)
            dumbo3435_connections[dumbo3435_46] = conn
            
            -- Connection cleanup on NPC removal (prevents memory leak)
            dumbo3435_46.AncestryChanged:Connect(function(_, parent)
                if not parent then
                    if dumbo3435_connections[dumbo3435_46] then
                        pcall(function() dumbo3435_connections[dumbo3435_46]:Disconnect() end)
                        dumbo3435_connections[dumbo3435_46] = nil
                    end
                end
            end)
        end
    end
    task.spawn(function()
        local dumbo3435_49 = 0;
        while dumbo3435_46["Parent"] and (dumbo3435_47["Position"]['Y'] > -100) and (dumbo3435_49 < 50) do
            dumbo3435_47["CFrame"] = dumbo3435_47["CFrame"] - Vector3.new(0, 150, 0);
            task.wait(0.1);
            dumbo3435_49 = dumbo3435_49 + 1;
        end
        dumbo3435_34[dumbo3435_46] = nil;
    end);
end

-- Checks if passenger is near any desk counter
local function isNPCNearDesk(npc)
    local hrp = npc:FindFirstChild("HumanoidRootPart")
    if not hrp then return false end
    
    local npcPos = hrp.Position
    local idPos = dumbo3435_25 and dumbo3435_25.Parent and dumbo3435_25.Parent.Position
    local mdPos = dumbo3435_23 and dumbo3435_23.Parent and dumbo3435_23.Parent.Position
    
    if idPos then
        local dist = (Vector3.new(npcPos.X, 0, npcPos.Z) - Vector3.new(idPos.X, 0, idPos.Z)).Magnitude
        if dist < 6.0 then return true end
    end
    if mdPos then
        local dist = (Vector3.new(npcPos.X, 0, npcPos.Z) - Vector3.new(mdPos.X, 0, mdPos.Z)).Magnitude
        if dist < 6.0 then return true end
    end
    
    return false
end

-- Strictly check if NPC is a valid living passenger who is NOT a criminal
local function isNPCNormalPassenger(npc)
    local humanoid = npc:FindFirstChildOfClass("Humanoid")
    if not humanoid or humanoid.Health <= 0 then return false end
    
    local properties = npc:FindFirstChild("Properties")
    local randVars = properties and properties:FindFirstChild("RandomVariables")
    if not randVars then return false end
    
    -- Check if they are classified as a criminal
    local fakePassport = randVars:FindFirstChild("FakePassport")
    local contrabandReal = randVars:FindFirstChild("ContrabandReal")
    local hasWeapon = randVars:FindFirstChild("Weapon") or 
                      randVars:FindFirstChild("WeaponReal") or 
                      randVars:FindFirstChild("HasWeapon") or 
                      randVars:FindFirstChild("WeaponContraband") or
                      randVars:FindFirstChild("Contraband")
                      
    if (fakePassport and fakePassport.Value == true) or
       (contrabandReal and contrabandReal.Value == true) or
       (hasWeapon and hasWeapon.Value == true) then
        return false -- Criminal!
    end
    
    return true -- Innocent!
end

-- Mathematically check if the passenger is actually waiting in line (on the queue side of the desk)
local function isPassengerInQueue(npc, buttonPos)
    local hrp = npc:FindFirstChild("HumanoidRootPart")
    if not hrp then return false end
    
    local npcPos2D = Vector3.new(hrp.Position.X, 0, hrp.Position.Z)
    local buttonPos2D = Vector3.new(buttonPos.X, 0, buttonPos.Z)
    
    -- Must be within 12 studs of the desk button podium
    local dist = (npcPos2D - buttonPos2D).Magnitude
    if dist > 12.0 then return false end
    
    -- Dot product check: clerk stands towards midpoint (clerkDir)
    -- Passengers wait on opposite side. Angle must be > 90 deg (Dot product < -0.2)
    local clerkDir = (Vector3.new(dumbo3435_33.X, 0, dumbo3435_33.Z) - buttonPos2D).Unit
    local npcDir = (npcPos2D - buttonPos2D).Unit
    local dot = npcDir:Dot(clerkDir)
    
    return dot < -0.2
end

print("Secure the Airport: Variables initialized! Starting main loop...");

while true do
    -- If script active local variable is set to false, end loop immediately (safe from race conditions)
    if not scriptActive then
        break
    end

    -- Passive check/update of NPC speeds (innocents accelerated, hostiles/criminals frozen)
    -- This section runs CONTINUOUSLY regardless of Autofarm toggle state so you don't lose the game!
    if dumbo3435_17 then
        for dumbo3435_53, dumbo3435_54 in dumbo3435_17:GetChildren() do
            if (dumbo3435_54["Name"] == "EnemyNPC" or dumbo3435_54["Name"] == "FSEnemyNPC" or dumbo3435_54["Name"] == "BossNPC") then
                dumbo3435_45(dumbo3435_54);
            end
        end
    end
    
    if dumbo3435_16 then
        for dumbo3435_55, dumbo3435_56 in dumbo3435_16:GetChildren() do
            dumbo3435_45(dumbo3435_56);
        end
    end
    
    if dumbo3435_15 then
        for dumbo3435_57, dumbo3435_58 in dumbo3435_15:GetChildren() do
            if (not dumbo3435_34[dumbo3435_58]) then
                local dumbo3435_59 = dumbo3435_58:FindFirstChildOfClass("Humanoid");
                if dumbo3435_59 then
                    local properties = dumbo3435_58:FindFirstChild("Properties")
                    local randVars = properties and properties:FindFirstChild("RandomVariables")
                    
                    if randVars then
                        -- passenger properties loaded, check criminal state
                        local fakePassport = randVars:FindFirstChild("FakePassport")
                        local contrabandReal = randVars:FindFirstChild("ContrabandReal")
                        local hasWeapon = randVars:FindFirstChild("Weapon") or 
                                          randVars:FindFirstChild("WeaponReal") or 
                                          randVars:FindFirstChild("HasWeapon") or 
                                          randVars:FindFirstChild("WeaponContraband") or
                                          randVars:FindFirstChild("Contraband")
                                          
                        local isCriminal = (fakePassport and fakePassport.Value == true) or
                                           (contrabandReal and contrabandReal.Value == true) or
                                           (hasWeapon and hasWeapon.Value == true)
                                           
                        if isCriminal then
                            -- Criminal: Freeze them
                            dumbo3435_45(dumbo3435_58)
                        else
                            -- Innocent Passenger speed control based on MoveDirection
                            if not dumbo3435_58:GetAttribute("SpeedSetupDone") then
                                dumbo3435_58:SetAttribute("SpeedSetupDone", true)
                                
                                -- Initial speed setup
                                if dumbo3435_59.MoveDirection.Magnitude > 0 then
                                    dumbo3435_59.WalkSpeed = 24
                                else
                                    dumbo3435_59.WalkSpeed = 12
                                end
                                
                                -- Listen to MoveDirection changes to dynamically adjust WalkSpeed
                                -- If MoveDirection is > 0 (walking), speed them up.
                                -- If MoveDirection is 0 (standing in line/desk), release to default so they stand still!
                                local conn
                                conn = dumbo3435_59:GetPropertyChangedSignal("MoveDirection"):Connect(function()
                                    if dumbo3435_59.MoveDirection.Magnitude > 0 then
                                        dumbo3435_59.WalkSpeed = 24
                                    else
                                        dumbo3435_59.WalkSpeed = 12
                                    end
                                end)
                                dumbo3435_connections[dumbo3435_58] = conn
                                
                                dumbo3435_58.AncestryChanged:Connect(function(_, parent)
                                    if not parent then
                                        if dumbo3435_connections[dumbo3435_58] == conn then
                                            pcall(function() conn:Disconnect() end)
                                            dumbo3435_connections[dumbo3435_58] = nil
                                        end
                                    end
                                end)
                            else
                                -- Fallback updates for already setup characters
                                if dumbo3435_59.MoveDirection.Magnitude > 0 then
                                    dumbo3435_59.WalkSpeed = 24
                                else
                                    dumbo3435_59.WalkSpeed = 12
                                end
                            end
                        end
                    else
                        -- Properties not loaded yet (or static staff NPC) -> wait and do not freeze
                    end
                end
            end
        end
    end

    -- If Autofarm is disabled via GUI button, pause player movement and wait
    if not autofarmEnabled then
        pressingButtons = false
        task.wait(0.2)
        continue
    end

    if (dumbo3435_32 and dumbo3435_32["Enabled"] and dumbo3435_32:FindFirstChild("EndingWinFrame")) then
        local dumbo3435_50 = dumbo3435_32["EndingWinFrame"]:FindFirstChild("BarTop") and dumbo3435_32["EndingWinFrame"]["BarTop"]:FindFirstChild("TextLabel");
        if (dumbo3435_50 and dumbo3435_50["Visible"]) then
            local text = dumbo3435_50["Text"]:lower()
            if string.find(text, "airport") or string.find(text, "аэропорт") or string.find(text, "secured") or string.find(text, "защитили") or string.find(text, "выиграли") then
                task.wait(3);
                if dumbo3435_20 then
                    dumbo3435_20:FireServer();
                end
                break;
            end
        end
    end
    
    local dumbo3435_51 = dumbo3435_18["Character"];
    local dumbo3435_52 = dumbo3435_51 and dumbo3435_51:FindFirstChild("HumanoidRootPart");
    
    local dumbo3435_64 = nil;
    for dumbo3435_65, dumbo3435_66 in dumbo3435_29 do
        if (dumbo3435_66 and dumbo3435_66:FindFirstChild("FirePart") and dumbo3435_66["FirePart"]:FindFirstChild("IsOnFire") and (dumbo3435_66["FirePart"]["IsOnFire"]["Value"] == true)) then
            dumbo3435_64 = dumbo3435_66;
            break;
        end
    end
    
    if (dumbo3435_64 and dumbo3435_52) then
        local dumbo3435_67 = dumbo3435_51:FindFirstChild("Fire Extinguisher") or (dumbo3435_18["Backpack"] and dumbo3435_18["Backpack"]:FindFirstChild("Fire Extinguisher"));
        if (not dumbo3435_67 and dumbo3435_30) then
            for dumbo3435_68, dumbo3435_69 in dumbo3435_30:GetChildren() do
                local dumbo3435_70 = dumbo3435_69:FindFirstChild("InteractPart");
                local dumbo3435_71 = dumbo3435_70 and dumbo3435_70:FindFirstChild("ProximityPrompt");
                if (dumbo3435_71 and dumbo3435_71["Enabled"]) then
                    local standPos = dumbo3435_69:GetPivot().Position
                    local playerPos = standPos + Vector3.new(0, 2, 4) -- Stand 4 studs back
                    
                    -- Orient player and camera to look directly at the fire extinguisher box
                    dumbo3435_52["CFrame"] = CFrame.lookAt(playerPos, standPos)
                    dumbo3435_19["CFrame"] = CFrame.lookAt(playerPos + Vector3.new(0, 2, 2), standPos)
                    task.wait(0.4);
                    
                    local dumbo3435_72 = 0;
                    while not (dumbo3435_51:FindFirstChild("Fire Extinguisher") or (dumbo3435_18["Backpack"] and dumbo3435_18["Backpack"]:FindFirstChild("Fire Extinguisher"))) and (dumbo3435_72 < 15) do
                        triggerPrompt(dumbo3435_71);
                        task.wait(0.4);
                        dumbo3435_72 = dumbo3435_72 + 1;
                    end
                    break;
                end
            end
        end
        
        local dumbo3435_73 = dumbo3435_38("Fire Extinguisher");
        if (dumbo3435_73 and dumbo3435_73:FindFirstChild("Toggle")) then
            local dumbo3435_74 = dumbo3435_64:FindFirstChild("FirePart") or dumbo3435_64;
            local dumbo3435_75 = dumbo3435_74["Position"];
            local dumbo3435_76 = dumbo3435_75 + Vector3.new(0, 2, 4);
            dumbo3435_52["CFrame"] = CFrame.lookAt(dumbo3435_76, dumbo3435_75);
            dumbo3435_19["CFrame"] = CFrame.lookAt(dumbo3435_76 + Vector3.new(0, 2, 2), dumbo3435_75);
            task.wait(0.3);
            if dumbo3435_21 then
                dumbo3435_21:FireServer(dumbo3435_73, true);
            end
            dumbo3435_73["Toggle"]:FireServer(true);
            local dumbo3435_77 = 0;
            while dumbo3435_74:FindFirstChild("IsOnFire") and (dumbo3435_74["IsOnFire"]["Value"] == true) and (dumbo3435_77 < 35) do
                task.wait(0.2);
                dumbo3435_77 = dumbo3435_77 + 1;
            end
            if dumbo3435_21 then
                dumbo3435_21:FireServer(dumbo3435_73, false);
            end
            dumbo3435_73["Toggle"]:FireServer(false);
            local jail = workspace:FindFirstChild("WorkspaceScriptable") and workspace["WorkspaceScriptable"]:FindFirstChild("JailEssentials")
            local prisoner = jail and jail:FindFirstChild("Prisoner2")
            local dumbo3435_78 = prisoner and prisoner:FindFirstChild("HumanoidRootPart");
            if (dumbo3435_78) then
                dumbo3435_52["CFrame"] = dumbo3435_78["CFrame"];
            end
            task.wait(0.5);
            continue;
        else
            task.wait(1);
        end
    end
    
    if (dumbo3435_27 and dumbo3435_27["Enabled"] == true and dumbo3435_52 and dumbo3435_26) then
        local computerPos = dumbo3435_26:GetPivot().Position
        local playerPos = computerPos + Vector3.new(0, 2, 4)
        
        -- Orient player and camera to look directly at the computer screen
        dumbo3435_52["CFrame"] = CFrame.lookAt(playerPos, computerPos)
        dumbo3435_19["CFrame"] = CFrame.lookAt(playerPos + Vector3.new(0, 2, 2), computerPos)
        task.wait(0.5);
        
        while dumbo3435_27["Enabled"] == true do
            local dumbo3435_79 = dumbo3435_28 and dumbo3435_28:FindFirstChild("ProximityPrompt");
            if dumbo3435_79 then
                triggerPrompt(dumbo3435_79);
            end
            task.wait(0.3);
        end
        local jail = workspace:FindFirstChild("WorkspaceScriptable") and workspace["WorkspaceScriptable"]:FindFirstChild("JailEssentials")
        local prisoner = jail and jail:FindFirstChild("Prisoner2")
        local dumbo3435_78 = prisoner and prisoner:FindFirstChild("HumanoidRootPart");
        if (dumbo3435_78) then
            dumbo3435_52["CFrame"] = dumbo3435_78["CFrame"];
        end
        task.wait(0.5);
        continue;
    end
    
    if (dumbo3435_52) then
        local hasFire = dumbo3435_35()
        local compBroken = dumbo3435_27 and dumbo3435_27["Enabled"] == true

        -- Keep player at midpoint ONLY during active event resolving
        if (not pressingButtons and not hasFire and not compBroken) then
            if (((dumbo3435_52["Position"] - dumbo3435_33)["Magnitude"] > 4.0)) then
                dumbo3435_52["CFrame"] = cframeCenter;
                task.wait(0.1);
            end
        end
        
        -- Automatically fire button prompts if we are not computer-broken and there is no fire
        if (not compBroken and not hasFire) then
            -- Count passengers at each desk queue in a 12-stud horizontal radius
            local idQueue = 0
            local mdQueue = 0
            
            if dumbo3435_25 and dumbo3435_25.Parent then
                local idPos = dumbo3435_25.Parent.Position
                if dumbo3435_15 then
                    for _, npc in ipairs(dumbo3435_15:GetChildren()) do
                        if isNPCNormalPassenger(npc) and isPassengerInQueue(npc, idPos) then
                            idQueue = idQueue + 1
                        end
                    end
                end
            end
            
            if dumbo3435_23 and dumbo3435_23.Parent then
                local mdPos = dumbo3435_23.Parent.Position
                if dumbo3435_15 then
                    for _, npc in ipairs(dumbo3435_15:GetChildren()) do
                        if isNPCNormalPassenger(npc) and isPassengerInQueue(npc, mdPos) then
                            mdQueue = mdQueue + 1
                        end
                    end
                end
            end
            
            -- Decide where to stand based on passenger presence (Conditional Teleportation)
            local targetPodium = nil
            
            if idQueue == 0 and mdQueue == 0 then
                -- No passengers anywhere -> stay in the center
                targetPodium = "Center"
            elseif idQueue > 0 and mdQueue == 0 then
                -- Passengers only at ID -> stand at ID
                targetPodium = "ID"
            elseif mdQueue > 0 and idQueue == 0 then
                -- Passengers only at MD -> stand at MD
                targetPodium = "MD"
            else
                -- Passengers at both -> alternate every 2.5 seconds to service both queues
                local now = os.clock()
                if (now - stateStartTime > 2.5) then
                    stateStartTime = now
                    if currentPodium == "ID" then
                        currentPodium = "MD"
                    else
                        currentPodium = "ID"
                    end
                end
                targetPodium = currentPodium
            end
            
            if targetPodium == "Center" then
                pressingButtons = false
                -- Teleport back to center when no passengers are waiting
                if (((dumbo3435_52["Position"] - dumbo3435_33)["Magnitude"] > 4.0)) then
                    dumbo3435_52["CFrame"] = cframeCenter
                    task.wait(0.1)
                end
            elseif targetPodium == "ID" and dumbo3435_25 then
                pressingButtons = true
                local targetCFrame = cframeID
                local horizontalDist = (Vector3.new(dumbo3435_52.Position.X, 0, dumbo3435_52.Position.Z) - Vector3.new(targetCFrame.Position.X, 0, targetCFrame.Position.Z)).Magnitude
                if horizontalDist >= 2.0 then
                    pcall(function()
                        dumbo3435_52.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                        dumbo3435_52.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                    end)
                    dumbo3435_52["CFrame"] = targetCFrame
                    task.wait(0.08)
                    dumbo3435_52["CFrame"] = targetCFrame
                    task.wait(0.1)
                end
                
                -- CCTV camera looking down the ID queue to see wait states
                pcall(function()
                    local buttonPos = dumbo3435_25.Parent.Position
                    local lookDir = (buttonPos - targetCFrame.Position).Unit
                    local camPos = targetCFrame.Position - lookDir * 4.0 + Vector3.new(0, 5.0, 0)
                    local camTarget = buttonPos + lookDir * 10.0
                    dumbo3435_19["CFrame"] = CFrame.lookAt(camPos, camTarget)
                end)
                
                -- Click prompt once every 1.0 second to prevent spam glitches
                local now = os.clock()
                if now - lastTriggerTimeID > 1.0 then
                    lastTriggerTimeID = now
                    triggerPrompt(dumbo3435_25)
                end
            elseif targetPodium == "MD" and dumbo3435_23 then
                pressingButtons = true
                local targetCFrame = cframeMD
                local horizontalDist = (Vector3.new(dumbo3435_52.Position.X, 0, dumbo3435_52.Position.Z) - Vector3.new(targetCFrame.Position.X, 0, targetCFrame.Position.Z)).Magnitude
                if horizontalDist >= 2.0 then
                    pcall(function()
                        dumbo3435_52.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                        dumbo3435_52.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                    end)
                    dumbo3435_52["CFrame"] = targetCFrame
                    task.wait(0.08)
                    dumbo3435_52["CFrame"] = targetCFrame
                    task.wait(0.1)
                end
                
                -- CCTV camera looking down the MD queue to see wait states
                pcall(function()
                    local buttonPos = dumbo3435_23.Parent.Position
                    local lookDir = (buttonPos - targetCFrame.Position).Unit
                    local camPos = targetCFrame.Position - lookDir * 4.0 + Vector3.new(0, 5.0, 0)
                    local camTarget = buttonPos + lookDir * 10.0
                    dumbo3435_19["CFrame"] = CFrame.lookAt(camPos, camTarget)
                end)
                
                -- Click prompt once every 1.0 second to prevent spam glitches
                local now = os.clock()
                if now - lastTriggerTimeMD > 1.0 then
                    lastTriggerTimeMD = now
                    triggerPrompt(dumbo3435_23)
                end
            end
        else
            pressingButtons = false
        end
    end
    
    task.wait(0.1);
end
